# ============================================
# SMD Frontend Environment Configuration
# ============================================
# IMPORTANT: Copy this file to .env.local (NOT .env)
# .env.local will NOT be committed to Git

# API Gateway URL (MUST go through Gateway, not direct to Core Service)
VITE_API_GATEWAY_URL=http://localhost:8888

# API Timeout (ms)
VITE_API_TIMEOUT=10000

# App Environment
VITE_APP_ENV=development

# ============================================
# SETUP INSTRUCTIONS:
# ============================================
# 1. Copy this file: cp .env.example .env.local
# 2. Update VITE_API_GATEWAY_URL if needed
# 3. DO NOT commit .env.local to Git
#
# IMPORTANT NOTES:
# - In development: Gateway runs on http://localhost:8888
# - In Docker: Gateway runs on http://localhost:8888
# - In production: Update to your domain (e.g., https://api.yourdomain.com)
# - DO NOT use http://localhost:8081 (bypasses Gateway - violates microservice pattern)
#
# Port Reference:
# - Gateway: 8888 (⭐ Use this)
# - Core Service: 8081 (Internal only)
# - Frontend Admin: 3000
# - Frontend Student: 3001
# - Vite Dev Server: 5173
